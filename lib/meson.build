sources = ['Chunker.cc']

chunklib = shared_library('chunker',
                        sources,
                        include_directories: inc,
                        install: false)
staticchunklib = static_library('chunker',
                        sources,
                        include_directories: inc,
                        install: false)

